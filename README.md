# Signature Extraction API (Python + FastAPI + PyMuPDF + OpenCV)

A lightning-fast signature extractor for Bangladeshi NID / Birth / Certificate PDF files.

✅ Works with scanned PDFs  
✅ Works with embedded images  
✅ Detects bottom-most signature  
✅ Accurate OpenCV + PyMuPDF based  
✅ Deploy-ready for Railway / Render / PythonAnywhere  
✅ Returns Base64 signature data URI

---

# 🚀 API Endpoint

**POST** `/signature`

### Request
